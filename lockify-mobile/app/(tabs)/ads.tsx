import { View, Text } from 'react-native';

export default function TabScreen() {
  return (
    <View style={{ flex: 1, backgroundColor: '#000', alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ color: '#fff', fontSize: 18 }}>Magic Ads Mobile</Text>
    </View>
  );
}
